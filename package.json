{
  "name": "knobs",
  "version": "0.0.2",
  "description": "Feature toggles",
  "main": "lib/index.js",
  "scripts": {
    "clean": "rm -rf ./lib",
    "compile": "npm run clean && ./node_modules/.bin/babel src --out-dir lib",
    "watch": "npm run clean && ./node_modules/.bin/babel --watch src --out-dir lib",
    "test": "./node_modules/.bin/mocha"
  },
  "repository": {
    "type": "git",
    "url": "git@github.ibm.com:Stack-The-Planet/toggles.git"
  },
  "keywords": [
    "feature",
    "toggle"
  ],
  "author": "Preston Pham",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.1.18",
    "babel-preset-es2015": "^6.1.18",
    "mocha": "^2.3.3"
  }
}
